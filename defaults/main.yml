---
kibana_version: "4.6"

kibana_server_port: 5601
kibana_server_host: "0.0.0.0"

kibana_elasticsearch_url: "http://localhost:9200"

kibana_plugins: []
## Add plugins by name (from --list) and src (used for --install)
# kibana_plugins:
#   - name: sense
#     src: elastic/sense
